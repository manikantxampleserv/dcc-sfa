import { Router } from 'express';
// import { authenticateToken } from '../../middlewares/auth.middleware';
// import { zonesController } from '../controllers/zones.controller';
// import { createZoneValidation } from '../validations/zones.validation';
// import { validate } from '../../middlewares/validation.middleware';

const router = Router();

// router.post(
//   '/zones',
//   authenticateToken,
//   createZoneValidation,
//   validate,
//   zoneController.createDepots
// );

// router.get(
//   '/zones/:id',
//   authenticateToken,
//   validate,
//   zoneController.getDepotsById
// );
// router.get('/zones', authenticateToken, zoneController.getDepots);

// router.put('/zones/:id', authenticateToken, zoneController.updateDepots);

// router.delete('/zones/:id', authenticateToken, zoneController.deleteDepots);

export default router;
