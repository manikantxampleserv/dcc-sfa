{
  "name": "dcc-sfa",
  "version": "1.0.0",
  "description": "DCC SFA - Sales Force Automation System",
  "private": true,
  "workspaces": [
    "dcc-sfa-be",
    "dcc-sfa-fe"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "install:all": "npm install && npm install --workspaces",
    "clean:files": "npx rimraf dist node_modules dcc-sfa-be/dist dcc-sfa-fe/dist dcc-sfa-be/node_modules dcc-sfa-fe/node_modules dcc-sfa-be/package-lock.json dcc-sfa-fe/package-lock.json",
    "clean:ports": "npx kill-port 4000 5173",
    "clean": "npm run clean:ports && npm run clean:files"
  },
  "devDependencies": {
    "kill-port": "^2.0.1",
    "prettier": "^3.0.0",
    "rimraf": "^5.0.0",
    "turbo": "^2.5.8"
  },
  "packageManager": "npm@10.0.0",
  "engines": {
    "node": ">=16.0.0"
  }
}
